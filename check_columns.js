const { createClient } = require('@supabase/supabase-js'); const supabase = createClient('https://vuaayfuhqbrkvwutcidw.supabase.co', 'sb_publishable_-qWiT8Yk4ZjS9h0LcfbUjA_HXs5sdWk'); async function check() { const { data } = await supabase.from('barbershops').select('*').limit(1); if(data && data.length > 0) console.log('Columns:', Object.keys(data[0])); else console.log('No data found'); } check();
